{
  "name": "bread-cafe-project",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build:staging": "JEKYLL_ENV=staging bundle exec jekyll b -s source",
    "build:production": "JEKYLL_ENV=production bundle exec jekyll b -s source",
    "dev": "bundle exec jekyll s --livereload -s source"
  }
}
